



const stateDefault = {
    like: true
}

export const insReducer = (state = stateDefault, aciton) => {
    switch (aciton.type) {
        case 'LIKE': {
            
        }

        default: return state;
    }

}